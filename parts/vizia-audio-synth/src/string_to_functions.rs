use crate::asdr::ADSR;
use crate::filters::Filter;
use crate::sampler::Sample;

mod oscillator;
mod asdr;
mod filters;
mod sampler;


//Nota si va muy lento hacer algo caracter por caracter o comparar las diferencias (mas lo segundo la vd)
fn str_to_fn(string: &str) -> f32 {
    for c in string {

    }
}
